# Voice → Instruments

Adds a third Voice workspace option where a user performs instrument ideas with their mouth/voice, renders each sketch as a selected instrument, layers the rendered parts, and can ask Pie to build a finished instrumental song from that rough arrangement.

Initial instrument targets: bass, drums, guitar, keys, strings, synth, percussion.

Safety/UX: local reset only clears this workspace and does not delete saved Songs.
